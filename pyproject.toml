[project]
name = "a2a-runtime"
version = "0.1.0rc5"
description = "Local Python runtime foundations for Markdown file-based A2A agents."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
a2a-agent = "a2a_runtime.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
