---
description: A2A 触发器（精简常驻版）。识别用户是否真要走 A2A 流程；只在显式触发时加载完整规则 ai-agents-full，平时不打 [A2A] 头、不读 state.md，按常规对话处理。
alwaysApply: true
---

# A2A 触发器（精简常驻版）

本项目存在 `.ai-agents/` A2A 多 Agent 工作流系统。完整规则有 250+ 行硬约束，按需加载以节省 token。

## 触发条件（满足任一 → 加载完整规则 `ai-agents-full`）

1. 用户消息显式包含以下任一：
   - 关键词：`走 A2A` / `启动 A2A` / `A2A Task` / `a2a-agent` / `推进 task` / `跑流程`
   - **简写开关**（与「走 A2A」等价，避免用户打 `a2a xxx` 却不触发）：整条消息在 trim 后以 `a2a` 或 `A2A` 开头，且紧跟**空白**或**英文逗号**（例：`a2a 1+1`、`A2A, 查状态`）。不要求大小写混写；**不以** `a2a` 后直接黏连中文（如 `a2a是什么`）触发，以免纯概念问答误开
2. 用户消息出现 `[A2A]` 回复头格式
3. 用户明确要求操作 `.ai-agents/workspace/` 下的 `task.md` / `state.md` / `artifacts/**` / `blockers/**` / `human-reviews/**` / `messages/**`
4. 用户调用 `a2a-agent` CLI 后让你解读 / 推进结果

## 触发后行为

主动 fetch `.cursor/rules/ai-agents-full.mdc` 完整内容，按其执行：每次回复以 `[A2A]` 头开头、4 步识别 Task、5 条写代码门禁、Blocker 两阶段、Review 双步等全部硬约束。

## 不触发时的行为

- **不**打 `[A2A]` 头
- **不**读 `state.md` / `task.md`
- **不**强制双步流转 / 双门禁等 A2A 规则
- 按常规 Cursor 对话处理（仍受其他通用规则约束，如 quality-gate / code-format-generation 等）

## 不确定时的兜底

如用户意图模糊（既像普通问题又像 A2A 操作），先按常规处理；若回答中需要触碰 `.ai-agents/workspace/` 内容，再加载 `ai-agents-full` 后重新组织答复。**宁可漏触发再补，不要默认每次都拉完整规则。**

## 边界

- 不触发 ≠ 关闭 A2A：完整规则始终可用，仅延迟到真正需要时加载
- 触发器本身（本文件）保持常驻，长度控制在 50 行以内
- 完整规则 `ai-agents-full.mdc` 的硬约束不变，仅改变加载时机
